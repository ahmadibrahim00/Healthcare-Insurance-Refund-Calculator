image: maven:latest

stages:
  - build
  - test

java-build:
  # select the most appropriate image for your project
  stage: build
  script:
    - mvn package -DskipTests=true

java-run :
  stage: test
  script: 
    java -jar "target\Remboursement-1.0-SNAPSHOT-shaded.jar" "Reclamation.json" "Remboursement.json"